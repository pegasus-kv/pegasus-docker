version: "3.3"

services:
  zookeeper:
    image: zookeeper:3.4
    ports:
      - "2181"
    restart: on-failure
    hostname: zookeeper
    environment:
      ZOO_MY_ID: 1
      ZOO_SERVERS: server.1=zookeeper:2888:3888
